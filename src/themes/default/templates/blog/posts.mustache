{{<html}}
{{$main}}

<!-- UX REVAMP: clean paginated archive -->
<div class="posts-section">
  <div class="section-head">
    <span class="section-title">Writing</span>
    <a href="/happyapps/rss.xml" class="section-more">RSS</a>
  </div>

  <div class="post-list">
    {{#empty(pagination.data.post)}}
    {{/empty(pagination.data.post)}}
    {{^empty(pagination.data.post)}}
    {{#pagination.data.post}}
    {{> partials.blog.post}}
    {{/pagination.data.post}}
    {{/empty(pagination.data.post)}}
  </div>

  {{^empty(pagination.links.post)}}
  <div class="pagination-bar">
    {{#pagination.links.post}}
    {{> partials.pagination}}
    {{/pagination.links.post}}
  </div>
  {{/empty(pagination.links.post)}}
</div>

<!-- OLD POSTS LISTING (commented out, kept for reference)
<section class="hero primary">
    <div class="wrapper">
        <header>
            <svg>...</svg>
            <p>Blog</p>
            <h1>Posts</h1>
            <p>Browse all posts</p>
        </header>
    </div>
</section>

<section>
    <div class="wrapper">
    {{#empty(pagination.data.post)}}Empty.{{/empty(pagination.data.post)}}
    {{^empty(pagination.data.post)}}
        <div class="grid grid-221">
        {{#pagination.data.post}}{{> partials.blog.post}}{{/pagination.data.post}}
        </div>
    {{/empty(pagination.data.post)}}
    {{^empty(pagination.links.post)}}
        <div class="pagination">
        {{#pagination.links.post}}{{> partials.pagination}}{{/pagination.links.post}}
        </div>
    {{/empty(pagination.links.post)}}
    </div>
</section>
-->

{{/main}}
{{/html}}
